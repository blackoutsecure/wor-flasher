{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "WoR-Flasher Configuration Schema",
  "description": "JSON schema for WoR-Flasher configuration parameters",
  "type": "object",
  "properties": {
    "target": {
      "type": "object",
      "properties": {
        "rpiModel": {
          "type": "integer",
          "enum": [3, 4, 5],
          "description": "Target Raspberry Pi model (3, 4, or 5)"
        },
        "device": {
          "type": "string",
          "description": "Target block device path (e.g. /dev/sda or /dev/disk4)"
        },
        "canInstallOnSameDrive": {
          "type": "boolean",
          "description": "Whether to install Windows directly onto target drive (true) or build recovery media (false)"
        }
      },
      "additionalProperties": false
    },
    "media": {
      "type": "object",
      "properties": {
        "winLang": {
          "type": "string",
          "description": "Windows language code (e.g. en-us)"
        },
        "bid": {
          "type": "string",
          "description": "Windows build ID (e.g. 22631.2861)"
        },
        "sourceFile": {
          "type": "string",
          "description": "Path to an existing local Windows ARM64 ISO file"
        },
        "downloadDir": {
          "type": "string",
          "description": "Directory path for downloads and extracted media"
        },
        "useCache": {
          "type": "integer",
          "enum": [0, 1, 2],
          "description": "Cache mode: 0 (redownload), 1 (reuse with manifest check), 2 (trust cache)"
        }
      },
      "additionalProperties": false
    },
    "customization": {
      "type": "object",
      "properties": {
        "applyCustomConfigTxt": {
          "type": "boolean",
          "description": "Whether to apply custom config.txt to the boot partition"
        },
        "configTxt": {
          "type": "string",
          "description": "Body of config.txt for the boot partition"
        },
        "oobeNetworkBypass": {
          "type": "boolean",
          "description": "Whether to bypass Windows OOBE network requirement"
        },
        "pi4AutoDisable3Gb": {
          "type": "boolean",
          "description": "Whether to disable 3GB RAM limit on Pi 4 after setup"
        },
        "pi4UefiShellUnlock": {
          "type": "boolean",
          "description": "Whether to use a one-time UEFI Shell handoff to persistently disable the Pi 4 RAM limit"
        },
        "uefiUseLatest": {
          "type": "boolean",
          "description": "Whether to query GitHub for latest UEFI firmware instead of pinned version"
        },
        "driversUseLatest": {
          "type": "boolean",
          "description": "Whether to query GitHub for latest drivers instead of pinned version"
        },
        "hideEmptyDrives": {
          "type": "boolean",
          "description": "Whether to hide empty card-reader slots in WoR-PE"
        }
      },
      "additionalProperties": false
    },
    "userAccount": {
      "type": "object",
      "properties": {
        "setupAccount": {
          "type": "boolean",
          "description": "Whether to setup a local administrator account during setup"
        },
        "username": {
          "type": "string",
          "description": "Username for optional local account"
        },
        "password": {
          "type": "string",
          "description": "Password for optional local account (omit or leave empty in committed configs)"
        },
        "localeSetup": {
          "type": "boolean",
          "description": "Whether to configure Windows locale and regional settings"
        },
        "locale": {
          "type": "string",
          "description": "Windows locale code (e.g. en-US or en-GB)"
        }
      },
      "additionalProperties": false
    },
    "execution": {
      "type": "object",
      "properties": {
        "dryRun": {
          "type": "boolean",
          "description": "Whether to run in dry-run mode without writing to storage"
        },
        "skipImageVerification": {
          "type": "boolean",
          "description": "Whether to skip post-flash verification"
        },
        "verifyTls": {
          "type": "boolean",
          "description": "Whether to verify TLS certificates during downloads"
        },
        "checkForUpdates": {
          "type": "boolean",
          "description": "Whether to report a newer published release before setup work starts. The check is read-only and never modifies this installation."
        },
        "noUpdate": {
          "type": "boolean",
          "description": "Legacy inverse of checkForUpdates; true disables the release check"
        },
        "logFile": {
          "type": "string",
          "description": "Optional custom path for installer output. Omit to use a timestamped log under the download directory's logs/ folder, with last-run.log refreshed for support."
        }
      },
      "additionalProperties": false
    },
    "notifications": {
      "type": "object",
      "description": "How the GUI announces the end of a flash. Only the completion sound is selectable; a failure always uses the platform's own error sound.",
      "properties": {
        "playSound": {
          "type": "boolean",
          "description": "Whether to play a sound when the flash finishes"
        },
        "showNotification": {
          "type": "boolean",
          "description": "Whether to post a desktop notification when the flash finishes (macOS Notification Center, or notify-send on Linux)"
        },
        "sounds": {
          "type": "object",
          "description": "Completion sound per host platform. A name this host cannot play falls back to the platform default.",
          "properties": {
            "macos": {
              "type": "string",
              "description": "Name of a sound in /System/Library/Sounds (e.g. Glass, Hero, Ping, Submarine)"
            },
            "linux": {
              "type": "string",
              "description": "freedesktop sound-theme event name (e.g. complete, bell, message)"
            },
            "windows": {
              "type": "string",
              "description": "Reserved: Windows is not a supported host, so no sound is played there"
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "system": {
      "type": "object",
      "description": "Advanced overrides for project runtime metadata. Defaults live in src/config/metadata.json and are loaded by src/lib/metadata.sh; ordinary configs should usually omit this block.",
      "properties": {
        "peInstallerUrl": {
          "type": "string",
          "description": "Download URL for WoR-PE installer package"
        },
        "peInstallerSha256": {
          "type": "string",
          "description": "SHA-256 digest for WoR-PE installer package"
        },
        "uefiVerPi3": {
          "type": "string",
          "description": "Pinned UEFI firmware version for Pi 3"
        },
        "uefiVerPi4": {
          "type": "string",
          "description": "Pinned UEFI firmware version for Pi 4"
        },
        "uefiVerPi5": {
          "type": "string",
          "description": "Pinned UEFI firmware version for Pi 5"
        },
        "driverVer": {
          "type": "string",
          "description": "Pinned Windows driver package version"
        },
        "armv80MaxBuild": {
          "type": "integer",
          "description": "Maximum Windows build supported on ARMv8.0 hardware"
        },
        "win11MinBuild": {
          "type": "integer",
          "description": "Minimum build ID for Windows 11"
        },
        "win10OldestBuild": {
          "type": "string",
          "description": "Oldest supported Windows 10 build ID"
        },
        "exampleBid": {
          "type": "string",
          "description": "Example build ID shown in prompts"
        },
        "armv80SafeBid": {
          "type": "string",
          "description": "Recommended safe build ID for ARMv8.0 hardware"
        },
        "repoSlug": {
          "type": "string",
          "description": "GitHub repository owner/repo slug checked for newer releases"
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
